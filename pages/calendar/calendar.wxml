<!-- 日历年月 -->
<view class='container'>
  <view class="consecutive-days"><text>已连续喂羊1天</text></view>
  <view class='calendar_title flex_nowrap'>
    <view class='icon' bindtap='lastMonth'>
      <van-icon name="arrow-left" color="gray" />
    </view>
    <text class="current-date">{{year}}年{{month}}月</text>
    <view class='icon' bindtap='nextMonth'>
      <van-icon name="arrow" color="gray" />
    </view>
  </view>

  <!-- 日历主体 -->
  <view class='calendar'>
    <view class='header'>
      <view wx:for='{{date}}' wx:key='{{index}}' class='{{(index == todayIndex) && isTodayWeek ? "weekMark" : ""}}'>
        <text>{{item}}</text>
      </view>
    </view>

    <view class='date-box'>
      <block wx:for='{{dateArr}}' wx:key='{{index}}'>
        <view class='[{{isToday == item.isToday ? "nowDay" : ""}}, {{item.disable ? "disable" : ""}}]'>
          <view class='date-head' bindtap='lookHuoDong' data-year='{{year}}' data-month='{{month}}' data-datenum='{{item.dateNum}}'>
            <text class='{{item.disable ? "disable" : ""}}'>{{item.dateNum}}</text>
          </view>
        </view>
      </block>
    </view>
    
    <view class='to-feed-sheep'>
      <van-button round type="primary" block bind:click="toFeedSheep">去喂羊</van-button>
    </view>
  </view>

  <view class='footer'>
    <view class="replenish">
      <view class="card">
        <text>我的补喂羊卡：0</text>
        <text>每邀请一个好友即可获得一张补喂羊卡</text>
      </view>
      <van-button round plain type="primary" size="small">邀请好友</van-button>
    </view>

    <view class="app-name">
      <text>天天浇水</text>
    </view>
  </view>
</view>